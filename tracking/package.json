{
  "name": "id",
  "version": "1.0.0",
  "description": "Simple tracking & analytics",
  "main": "app.js",
  "repository": "https://github.com/achook/id",
  "author": "achook",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.721.0",
    "uuid": "^8.3.0"
  },
  "scripts": {
    "test": "mocha tests/unit/",
    "build": "sam build --use-container",
    "deploy": "sam build --use-container && sam deploy",
    "dev": "sam build --use-container && sam local start-api"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4"
  }
}
